node_modules/
.next/
dist/
out/
.turbo/

# Secrets — never commit
.env
.env.local
.env.*.local
*.pem

# OS / editor
.DS_Store
*.log

# TypeScript incremental build state — regenerated by every type-check.
*.tsbuildinfo

# review-crew scaffolding (regenerated per run)
.review-crew-run/
